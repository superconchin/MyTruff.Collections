version: "3.9"

services:

  postgres:

    image: postgres:16

    restart: always

    environment:

      POSTGRES_USER: process.env.USERNAME

      POSTGRES_PASSWORD: process.env.PASSWORD

      POSTGRES_DB: process.env.DATABASE

    ports:

      - "5432:5432"